// TODO (Step 13): Single stock row inside Watchlist
// Props: symbol, price, change_pct, vol_ratio, status
export default function StockRow({ symbol, price, change_pct, vol_ratio, status }) {
  return <div>{/* TODO */}</div>
}
